# The dataset generator uses only the Python standard library (Python 3.9+).
# No external dependencies are required to generate data or run the tests.
#
# Optional, for later phases:
#   pytest        # to run the test suite with `pytest -q`
#   anthropic     # Phase 3: LLM-based narrative classification (optional augmentation)
